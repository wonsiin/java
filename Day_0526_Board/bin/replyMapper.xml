<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!-- namespace : interface의 full name parameterType : 파라미터로 전달되는 객체의 full 
	name insert, update, delete => resultType을 지정하지 않아야 한다. -->

<mapper namespace="net.scit.dao.ReplyMapper">	<!-- 자바의 인터페이스 -->

	<select id="listReply" resultType="Reply">
		SELECT
		replynum, writer, text,
		regdate
		FROM reply
		WHERE boardnum = #{boardnum}
	</select>

	<insert id="writeReply" parameterType="Reply">
		INSERT INTO
		reply(replynum, boardnum, writer, text)
		VALUES(reply_seq.nextval,
		#{boardnum}, #{writer}, #{text})
	</insert>

	<select id="selectReply" parameterType="string"
		resultType="Reply">
		SELECT *
		FROM reply
		WHERE replynum = #{replynum}
	</select>

	<update id="updateReply" parameterType="Reply">
		UPDATE reply
		SET text =
		#{text}
		WHERE replynum = #{replynum}
	</update>

	<delete id="deleteReply" parameterType="string">
		DELETE FROM reply
		WHERE
		replynum = #{replynum}
	</delete>

</mapper>